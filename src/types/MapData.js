/**
 * @property {number} height The height of the map (in pixels).
 * @property {number} width The width of the map (in pixels).
 * @property {object} bounds The GPS coordinates of the map edges.
 * @property {number} bounds.bottom The latitude of the bottom edge of the map.
 * @property {number} bounds.left The longitude of the left edge of the map.
 * @property {number} bounds.right The longitude of the right edge of the map.
 * @property {number} bounds.top The latitude of the top edge of the map.
 */
export const MapData = {}
