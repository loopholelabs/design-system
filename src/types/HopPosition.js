/**
 * @typedef HopPosition
 * @property {string} name The name of the position to be rendered in the label.
 * @property {number} x Horizontal position.
 * @property {number} y Vertical position.
 */
export const HopPosition = {}
